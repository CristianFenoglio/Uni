#include <iostream>
#include "complex_number.hpp"

int main(void){
    using com = complex<float>;
    com x1(1, 1);
    comx2(); 
    std::cout << x1 << " + " << x2 << " = " << x1+x2 << std::endl;
    com x3 = x1+x2;
    std::cout << "The cognugate of " << x3 << " is " << cogn(r3) << "; ";
    return 0;
}